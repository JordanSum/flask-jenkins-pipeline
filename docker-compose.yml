services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - databaseurl=postgresql://postgres:password@postgres:5432/postgres # This is a placeholder connection string. In production, use environment variables or secrets management to handle sensitive information.
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password # This is a placeholder. In production, use a secure password and consider using environment variables or secrets management.
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
